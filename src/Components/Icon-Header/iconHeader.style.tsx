import styled from "styled-components";

export const Container = styled.img`
    width: 35px;
    height: 35px;
    margin: 0 10px;
    cursor: pointer;

    &:hover{
        transform: scale(1.1);
    }
`